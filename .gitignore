# Environment
.env
.env.local
.env.production

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/

# Node
node_modules/
.next/
out/
tsconfig.tsbuildinfo

# IDE
.vscode/
.idea/
*.swp
*.swo

# Docker
docker-compose.override.yml

# OS
.DS_Store
Thumbs.db

# Credentials / secrets (never commit)
*.xlsx
~$*
SocialMedia_Credentials.xlsx

# Data
*.log
frontend.log
*.sqlite3

# Production-like diagnostics (P0-13 — classify/govern before ever tracking;
# explicit paths kept alongside the bare names so these stay ignored even if
# the broad patterns above are ever narrowed)
BC-COLUMNS.txt
FABRIC-TABLES.txt
docs/build-files/BC-COLUMNS.txt
docs/build-files/FABRIC-TABLES.txt

# Sample reels are working artifacts, not source
samples/*.mp4
